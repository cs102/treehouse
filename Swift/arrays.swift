import UIKits

var todo = ["return books", "cook dinner", "write post"]
todo

var todo: [String] = ["return books", "cook dinner", "write post"]

todo += ["Shower", "Brush hair"]

todo[0] //first item in the array

todo.count // count is a property that tell us number or items 