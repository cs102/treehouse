import UIKit

var levelScore = 0

levelScore = levelScore + 1
levelScore

levelScore++
var totalScore = 0
totalScore = ++levelScore
totalScore
levelScore

levelScore++
var totalScore = ++levelScore

// ++ increment unary operator
// -- decrement unary operator
// - negative operator
// ! Not operator

let on = true
let off = !on

levelScore = 10
levelScore = -levelScore
//turns a positive value into a negative value.

// If the operator is written before the variable, 
// it increments the variable before returning its value.

// If the operator is written after the variable, 
// it increments the variable after returning its value.
